import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:{
        alignItems: 'center',
        paddingTop: 40,
        paddingHorizontal: 0,
        paddingBottom: 30,
    },
    top:{
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 10,
    },
    title:{
        fontSize: 18,
        color: '#FFF',
        fontWeight: 'bold',
        marginLeft: 8,
    },
});

export default styles;